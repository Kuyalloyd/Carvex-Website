import React from 'react';

export default function AdminNavbar({ stats: externalStats = null, loading: externalLoading = null }) {
    return (
        <div className="admin-navbar">
            {/* Navigation removed */}
        </div>
    );
}
